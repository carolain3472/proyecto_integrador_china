import {Register_form} from '../components/register_form';

/**
 * Componente Register_template.
 * 
 * Este componente representa la plantilla de la página de registro.
 * Renderiza el formulario de registro utilizando el componente Register_form.
 */

export function Register_template() {
  return <Register_form/>
}
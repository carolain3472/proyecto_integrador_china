import { Logout_china } from "../components/logout_china"

/**
 * Plantilla de cierre de sesión.
 * 
 * Esta función es una plantilla que muestra el componente `Logout_china`,
 * el cual se encarga del proceso de cierre de sesión. Al utilizar esta plantilla,
 * se renderizará el componente `Logout_china` para permitir al usuario cerrar sesión
 * de manera controlada.
*/

export function Logout_template() {
  return <Logout_china/>
  
}

